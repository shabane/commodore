commands:
    - key: /start
      messages:
        - "Welcome to the test bot! Try typing /help or /admin."

    - key: /help
      messages:
        - "You can use /admin to manage this bot if you are authorized."

    - key: /admin
      admins:
        - "m_shabane"
      plugins:
        - "admin"

wrong_command:
  messages:
    - "Sorry, I don't understand that command. Try /start."
